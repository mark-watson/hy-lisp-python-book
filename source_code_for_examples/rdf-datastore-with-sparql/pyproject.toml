[project]
name = "rdf-datastore-with-sparql"
version = "0.1.0"
description = "Simple in-memory RDF triple store with partial SPARQL query support"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hy>=1.1.0",
]

[tool.uv]
exclude-newer = "2 days"
